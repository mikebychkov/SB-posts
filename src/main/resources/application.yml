server:
  port: "5001"

spring:
  jpa:
    generate-ddl: "true"
    hibernate:
      ddl-auto: "update"
    show-sql: "true"
    properties:
      hibernate:
      dialect: "org.hibernate.dialect.PostgreSQL10Dialect"
  datasource:
    url: "jdbc:postgresql://localhost:5432/sb_posts"
    username: "postgres"
    password: "Pa$$w0rd"

management:
  endpoints:
    web:
      exposure:
        include: "*"