spring:
  application:
    name: posts
  profiles:
    active:
      default
  cloud:
    kubernetes:
      reload:
        enabled: true
        #strategy: shutdown
      config:
        sources:
          - name: config
